import { AddcontentDirective } from './addcontent.directive';

describe('AddcontentDirective', () => {
  it('should create an instance', () => {
    const directive = new AddcontentDirective();
    expect(directive).toBeTruthy();
  });
});
